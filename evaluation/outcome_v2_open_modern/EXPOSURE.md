# Open-modern execution record

The protocol was committed and pushed at
`90c3e644efe7588b0dd6d8617e8811890b52d98a` before the pinned answer file was
downloaded.

On 2026-07-16, a delegated schema check requested the first data row after the
header. The source is reverse-chronological, so this displayed one realized
label from the 2022 season. No other 2021 or 2022 labels or outcome summaries
were intentionally inspected.

Consequences:

- Keep the exposed row in the exact frozen cohort. Do not exclude it or tune to
  it.
- Describe the 2021-2022 result as a protocol-frozen historical holdout, not as
  a literally unopened test.
- Seal the remaining test labels away from development and score them only
  after the model and scoring procedure are frozen.
- Reserve any untouched-performance claim for a future prospective cohort.

Structural erratum: the source contains complementary outcome indicators, not
final scores. The protocol's non-tie rule is implemented as requiring exactly
one team outcome indicator to equal one.
